start:
    - insert:
        - bad mouse
        - good thing
        - somethng else

    - test:
        on condition: $'good' in _$
        send to: test_good

    - test:
        on condition: $'bad' in _$
        send to: test_bad

    - assert: somethng else

test_good:
    - assert: good thing

test_bad:
    - assert: bad mouse