start:
    - insert: { extension: .tar }
    - insert: [ samples/archives/*$extension$.*bz2 ]
    - read from file_list:
    - insert: $_$   # Expand list to item
    - insert:       # Test both local and remote
        - $_$
        - https://raw.githubusercontent.com/Openpipe/openpipe/master/$_$
    - print: Testing read from $_$
    - write to context: $_$
    - read from file: $_$
    - assert:
            - name: test.json
            - name: test.txt
            - name: test.xml
            - name: test.yaml
