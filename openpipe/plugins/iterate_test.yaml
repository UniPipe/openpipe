start:
    - send to segment:
        - dict iteration test
    - iterate: [a, b]
    - print:
    - assert: [a, b]

dict iteration test:
    - iterate: {a: 1, b: 5}
    - print:
    - assert:
        - {key: a, value: 1}
        - {key: b, value: 5}
