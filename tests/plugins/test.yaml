start:
    - insert:
        - bad mouse
        - good thing
        - somethng else

    - test:
        on_condition: $'good' in _$
        send_to: test_good

    - test:
        on_condition: $'bad' in _$
        send_to: test_bad

    - assert: somethng else

test_good:
    - assert: good thing

test_bad:
    - assert: bad mouse